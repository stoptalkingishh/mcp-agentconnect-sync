# agentconnect-temporal

Durable execution engine for the AgentConnect backplane. See `docs/BACKPLANE_SPEC_TEMPORAL.md`.

Not to be confused with the standalone `agentconnect-temporal` repo (router-era `AgentRuntime` substrate).
